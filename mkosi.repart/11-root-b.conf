[Partition]
Type=root-x86-64
Format=ext4
Label=root-b
# Don't copy files initially - this will be populated during first update
SizeMinBytes=8G
SizeMaxBytes=8G
# This partition starts as the backup/inactive partition
ReadOnly=no
